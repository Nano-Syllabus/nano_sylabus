import { RevisionDocsSkeleton } from "@/components/revision-docs-client";

/** Revision is the docs: a tree and one document, so that is what loads. */
export default function NotesLoading() {
  return <RevisionDocsSkeleton />;
}
