# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Every NEXT_DIST_DIR this repo builds into. The suffixed ones exist so a
# production build can run without replacing the dev server's chunk manifests
# (see next.config.ts), and each is ~400MB of output.
.next
.next-*
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
test-results/
playwright-report/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env
.env.production
.env.vercel.check
.env*.bak*
local_keys.txt
vercel_keys.txt
test-*.mjs
update-thinking-steps.js
app/api/test-stream/
app/api/test-tenant-stream/
public/test-stream.html
exam-prep-hub/

# Perf harness artifacts (Lighthouse runs, baseline build snapshot)
perf-results/

# TypeScript's incremental build cache. Regenerated by every `tsc`/`next build`,
# machine-specific, and 300KB of churn in the diff of anything that touches types.
tsconfig.tsbuildinfo
